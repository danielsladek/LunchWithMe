import { action } from "./actions";

const initialState = {
    events: []
};

 export const eventFeedReducer = (state = initialState) => {
    switch (action.type) {
     
        default:
            return state;
    }
};
